# ─────────────────────────────────────────────────────────────────────────────
# Audio Sentinel — ESP32-S3 + INMP441 audio monitor for Home Assistant.
#
# DEVICE / CONSUMER yaml. Pulls all reusable logic (packages + the audio_sentinel
# external component) from the public repo via github://, pinned to a tag.
# Secrets stay LOCAL here and are injected into the remote packages as
# substitutions (remote git packages cannot use !secret).
#
# For local development of the packages/component themselves, build
# audio-sentinel.dev.yaml instead (it includes them from this working tree).
# ─────────────────────────────────────────────────────────────────────────────

substitutions:
  name: audio-sentinel
  friendly_name: "SG Baby Audio Sentinel S3"
  device_description: "Audio Sentinel"

  # I²S wiring (INMP441)
  i2s_bclk: GPIO10
  i2s_lrclk: GPIO11
  i2s_din: GPIO04
  pin_led: GPIO21

  # Audio capture
  sample_rate: "16000"
  meas_duration_ms: "125"
  live_interval_ms: "250"

  # DSP tuning
  hysteresis_db: "1.5"
  initial_floor_db: "-60"
  floor_drift_db: "0.001"

  # Library version — bump this ONE value to take a new release
  sentinel_version: v1.1.6

  # Network
  static_ip: 192.0.2.50
  ap_ssid: "Sg-Baby-Audio-Sentinel-S3"

  # Secrets injected into the remote network package (kept local; never in the repo)
  wifi_ssid: !secret wifi_ssid
  wifi_password: !secret wifi_password
  ap_password: !secret failsafe_ap_password   # shared fallback-AP password across devices
  api_key: !secret api_password
  ota_password: !secret ota_password

esphome:
  name: ${name}
  comment: ${device_description}
  friendly_name: ${friendly_name}

esp32:
  board: esp32-s3-devkitc-1
  framework:
    type: arduino

logger:
  level: INFO

# Reusable logic pulled from the public repo, pinned via ${sentinel_version}.
# git source (not github:// shorthand) so we can point path: at the components
# folder, which lives under audio-sentinel/ rather than the repo root.
external_components:
  - source:
      type: git
      url: https://github.com/dude84/esphome-audio-sentinel
      ref: ${sentinel_version}
      path: audio-sentinel/components
    components: [audio_sentinel]

# Expanded git package: one block, one ref, all package files.
packages:
  audio_sentinel:
    url: https://github.com/dude84/esphome-audio-sentinel
    ref: ${sentinel_version}
    refresh: 1d
    files:
      - audio-sentinel/packages/network.yaml
      - audio-sentinel/packages/mic.yaml
      - audio-sentinel/packages/sentinel.yaml
      - audio-sentinel/packages/diagnostics.yaml
